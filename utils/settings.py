# use debugger level
# 0~6
# no, noset, debug, info, warnning, error, critical
debugger = 6

# the INF
INF = 0x7f7f3f7f

# force divide debug
# 优先级最高，如果为 True 将不会进行分图判断
noDivide = True
# 如果开启 则会强制进入分图路由 调试所用
forceDivide = False
